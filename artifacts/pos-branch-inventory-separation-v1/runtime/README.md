# pos-branch-inventory-separation-v1 runtime evidence

Generated by implementation pass on 2026-02-20.

## Automated evidence captured
- Build gates passed for cloud-api, online-store, web, desktop.
- Prisma client regenerated successfully.
- Governance impl-audit command executed (script currently prints workflow-only instructions).
- Guard scans show:
  - no runtime `console.*` in `apps/cloud-api/src` or `apps/desktop`
  - no `read_model_inventory.available` usage in POS/cloud sync paths
  - no stock-only filtering regex matches in sync/runtime paths

## Manual runtime evidence required to mark SAFE
- Employee decrement blocked in POS UI + API returns RBAC_FORBIDDEN
- Employee increment succeeds
- Admin decrement succeeds in POS and online-store admin
- Snapshot/delta payload includes product with branchQuantity=0
- Offline queue: enqueue while offline -> retry success -> queue record cleared

See the companion files in this folder for execution commands and placeholders.
